# Инструкции для AI-агентов (Проект Needle 2)

## 1. Архитектурное описание
Needle 2 — это компактная библиотека и runtime-движок для легковесного (45M) tool-calling LLM.
- `needle/agent`: Обработка запросов, вызовы инструментов (tool calling).
- `needle/model`: Архитектура Simple Attention Network (SAN), токенизатор, логика декодирования, квантования, дообучения (LoRA) и экспорта.
- `needle/environments`: Тестовые окружения для симуляции tool calling.
- Движок работает без сетевых вызовов при инференсе, все состояния сохраняются в ограниченном окне (256 токенов).
- Проект поддерживает тонкую настройку через LoRA с использованием фреймворка JAX.

## 2. Стандарты кодирования для русского языка (Кириллица)
- **Кодировки**: Во всём коде, при открытии файлов, парсинге JSON, сериализации и работе с потоками строго использовать `UTF-8` (без fallback на `ascii`).
- **JSON**: При вызове `json.dumps()` и аналогичных функций *всегда* передавать `ensure_ascii=False`.
- **Регулярные выражения**: Все regex должны учитывать Unicode (`re.UNICODE`) или избегать конструкций `[a-zA-Z]` в пользу более общих паттернов, если они могут обрабатывать кириллический текст.
- **Типизация и docstrings**: Использовать `typing`. Документация (в том числе к инструментам) может и должна содержать русский текст без ограничений, так как она используется для извлечения описаний в prompt.

## 3. Правила запуска тестов
- Тесты написаны с использованием `pytest`.
- Команда запуска тестов из корневой директории: `PYTHONPATH=$(pwd) pytest tests/`
- Перед коммитом необходимо проверить, что тесты `test_cyrillic.py` (и любые новые тесты на кириллицу) проходят успешно.
- В тестах запрещено использовать моки или проверки, зависящие от жестко закодированных английских слов, если тестируется функциональность, не зависящая от языка.

## 4. Ограничения и запреты
- ❌ Запрещено использовать `encoding="ascii"` или `decode("ascii")` при работе с текстовыми данными, промптами, аргументами тулов.
- ❌ Нельзя изменять/удалять существующие маски и спецтокены (`<|im_start|>`, `<tool_call>` и др.) в токенизаторе.
- ❌ При изменении длины контекста или логики сплиттинга не опираться на пробелы или длину английских слов.
